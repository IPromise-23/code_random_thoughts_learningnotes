from typing import List 

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            j = i + 1
            while j < len(nums):
                if nums[i] + nums[j] == target:
                    return [i,j]
                j += 1

# 本地测试入口：当脚本直接运行时，执行以下代码；被导入时不执行
if __name__ == '__main__':
    # 1. 实例化Solution类
    solution = Solution()
    
    # 2. 定义测试用例（可根据需要添加更多）
    test_cases = [
        # (nums列表, target值, 预期输出)
        ([2, 7, 11, 15], 9, [0, 1]),
        ([3, 2, 4], 6, [1, 2]),
        ([3, 3], 6, [0, 1])
    ]
    
    # 3. 遍历测试用例并验证结果
    for idx, (nums, target, expected) in enumerate(test_cases):
        result = solution.twoSum(nums, target)
        if result == expected:
            print(f"测试用例 {idx+1} 成功 ✅: nums={nums}, target={target}, 结果={result}")
        else:
            print(f"测试用例 {idx+1} 失败 ❌: nums={nums}, target={target}, 预期={expected}, 实际={result}")